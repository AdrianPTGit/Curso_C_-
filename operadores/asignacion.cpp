#include <iostream>

using namespace std;

int main()
{
    int a = 5;  // Declaramos una variable entera 'a' y le asignamos el valor 5
    int b = 10; // Declaramos una variable entera 'b' y le asignamos el valor 10
    a += b;     // Sumamos el valor de 'b' a 'a' (equivalente a a = a + b) 15

    // Igual con resta, multiplicacion y division.

    cout << "El valor de a es: " << a << endl;
    

}
